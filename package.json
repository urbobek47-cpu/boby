{
  "name": "boby-monorepo",
  "private": true,
  "scripts": {
    "build": "cd apps/storefront && npm install && npm run build",
    "dev": "cd apps/storefront && npm run dev"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
